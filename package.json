{
  "name": "dappnode-pioneers-new",
  "homepage": ".",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.3.1",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "gzip": "gzip -r -k build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "publish": "react-scripts build && rm build/static/js/*.map && node ./ipfsDwebUploader build/",
    "serve": "serve -s build",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "ipfs-http-client": "^29.1.1"
  }
}
